using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    //instantiate object liniarly
    //follow the cursor
    //if rope collide with knob with less than 3 rope already collide change all the rope tag to RopeMid[i] where i is the increasing variable
    //RopeMid[i] can allow only one collision with rope
    //if one player giveup or all numbeer of nodes are full then the winner is decided

   
   
}
